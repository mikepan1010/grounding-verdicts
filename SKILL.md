---
name: grounding-verdicts
description: Use when writing a verdict, finding, or status claim into a document, commit, memory, or report — especially when re-checking whether an older report, backlog, or inventory still holds, when claiming a mechanism/mapping/column does not exist, or when naming which table or definition is authoritative.
---

# Grounding Verdicts

裁決的依據必須是**判準的定義**，不是你查到的資料。資料查對了、判準歸屬錯了，
結論一樣是錯的，而且看起來完全合理。

## 交付契約（必填，不是建議）

每一條裁決都要能填滿這五格。**填不出來的那格就寫「未驗」，該條裁決只能是「未驗」。**

| 欄 | 要求 | 不接受 |
|---|---|---|
| **裁決** | 仍成立／已推翻／需修正／**未驗** | 省略 |
| **判準出處** | `檔:行` —— 定義這個判準的 production 程式碼 | 資料表名、欄位名、自己的推論 |
| **判準排除了什麼** | 該判準明文不涵蓋／刻意排除的東西 | 留空（留空＝沒讀過定義） |
| **權威依據** | 哪個 production 呼叫端在用它（grep 結果） | 「這張表名字對得上」 |
| **獨立第二法** | 換一種方法得到同一答案 | 同一支查詢再跑一次 |

## 三條硬規則

**① 權威由呼叫端決定，不由名字決定。**
要說「X 是這件事的權威／對照橋／單一來源」，先 grep 誰在用它。
同一個領域常有多張名字相似的表各司其職——挑錯的那張也能算出漂亮的數字。

**② 「不存在」宣告要窮舉載體，不能靠一次探針。**
說「沒有機制／沒有對映／沒有紀錄」之前，四類載體都要查過並寫下來：
**欄位／旁掛審計表／事件流／程式碼路徑**。
少查一類就只能寫「在 ○○ 沒找到」，**不得寫「不存在」**。

**③ 摘要與內文不一致 ＝ 未交付。**
總表、摘要、commit message 都是交付物。內文改對、標題留舊值，等於錯的那版才是別人會看到的。

## 紅旗——出現這幾句就停下來

- 「這張表名字就叫 X，那就是 X 的權威」
- 「數字變合理了，所以這次對了」 ← **合理性不是驗證**
- 「我查過了，沒有」 ← 查過哪一類載體？
- 「欄位叫 watermark，比它大就是落後」 ← 判準的排除條款讀了嗎
- 「相關的行我都 grep 出來了」 ← 過濾條件是你猜的
- 「時戳沒動，所以資料沒動」

## 舊報告／backlog 的數字

**當假設，不當事實。** 有日期的結論，日期本身就是保存期限。
重跑數字**不夠**——原報告的判準歸屬也要一起重驗；資料對而判準錯，重跑會得到「一字不差的錯誤」。

## 實測基礎

同一份四題考卷、10 次獨立作答（兩個模型各 5 次）：**8/40 格答錯，5/10 份交付物至少錯一條。**
零錯的那一題是「數一數有幾列」——單一探針就能答。
錯的 8 格全部集中在三種踩法：查錯權威（3）、沒讀判準的排除條款（2）、單一探針做不存在宣告（3）。
其中一份的內文正確、總表錯誤——那份交付出去的是錯的。

**同一份考卷、先讀本篇再作答，另外 10 次獨立作答（2026-08-25）：0/40 格答錯、0/10 份出錯，
五格欄位 10/10 填滿。** 判分規則在跑之前就登記好，事後沒有改動。

限制照實說：沒有安慰劑組（讀一份無關規範的對照），所以分不出功勞屬於這張檢查表本身，
還是「有規範可讀」這件事。另有一份總表與內文的百分比不一致（違反規則③）卻仍通過判分——
**判準本身對「數字」那一半驗得比對「權威歸屬」鬆**。
